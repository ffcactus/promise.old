package com.promise.auth.sdk.client;

import com.promise.auth.sdk.AccessPoint;
import com.promise.auth.sdk.Token;

public class AuthClient
{
    public boolean aa(Token token, AccessPoint accessPoint)
    {
        return true;
    }
}
